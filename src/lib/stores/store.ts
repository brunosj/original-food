import { writable } from 'svelte/store';

export const menuBackground = writable('var(--color-pri)');
export const background = writable('var(--color-sec)');
export const uspBackground = writable('var(--color-pri)');
export const font = writable('var(--color-ter)');
